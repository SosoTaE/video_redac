# ---------------------------------------------------------------------------
# ბილდის არტეფაქტები
# ---------------------------------------------------------------------------
build/
video_redac
*.o
*.d

# დებაგ-ბილდის დროებითი ფაილები
*.silent.mp4

# ---------------------------------------------------------------------------
# რენდერის შედეგები
# ---------------------------------------------------------------------------
#
# ყველა გამომავალი ვიდეო out/-ში იწერება. ისინი ყოველთვის თავიდან იწარმოება
# JSON-იდან, ამიტომ რეპოზიტორიაში მათი შენახვა მხოლოდ ზომას ზრდის
# (ამ პროექტში ~176 MB).
out/
*.mp4
*.nut

# ---------------------------------------------------------------------------
# მედია, რომელიც რეპოზიტორიას არ უნდა ჰქონდეს
# ---------------------------------------------------------------------------
#
# MLS-ის განცხადების ფოტოები: მესამე მხარის საავტორო უფლებებით დაცული და ~13 MB.
# ლოკალურად უნდა იყოს, რადგან listing/*.json მათ photos/NN.jpg-ით მიმართავს,
# მაგრამ საჯარო რეპოზიტორიაში მათი ატვირთვა არასწორია.
listing/photos/

# ---------------------------------------------------------------------------
# რედაქტორი / OS
# ---------------------------------------------------------------------------
*.swp
*~
.DS_Store
.vscode/
.idea/
